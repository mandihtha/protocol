{
  "name": "@mandihtha/protocol",
  "license": "MIT",
  "version": "2026.1.6-alpha",
  "description": "Mandihtha Protocol specification",
  "files": [
  ],
  "scripts": {
    "clean": "rm -rf website/build",
    "build": "cd website && hugo && rsync -a --delete --exclude='.git' build/ public/",
    "dev": "cd website && hugo server --buildDrafts"
  },
  "type": "module"
}
